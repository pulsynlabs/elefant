# Moved to references

This content is now a bundled reference. Use `reference({ name: "anti-patterns" })` to load it.

- **Reference:** `anti-patterns` (`src/agents/references/anti-patterns.md`)
- **Audience:** `all` (auto-loaded for all agents)
