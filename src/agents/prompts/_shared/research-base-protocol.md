# Moved to references

This content is now split across 4 bundled references. Use `reference({ name: "<id>" })` to load any of them:

- `field-notes-format` — frontmatter schema and section structure
- `field-notes-workflow` — write/search/read patterns and citation format
- `field-notes-output-format` — writer agent conventions
- `field-notes-search-patterns` — query patterns for field notes tools

**Audience:** `researcher`, `writer` (auto-loaded for researcher and writer agents)

See `src/agents/references/field-notes-*.md`.
