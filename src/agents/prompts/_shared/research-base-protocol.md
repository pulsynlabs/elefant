# Moved to references

This content is now split across 4 bundled references. Use `reference({ name: "<id>" })` to load any of them:

- `research-base-format` — frontmatter schema and section structure
- `research-base-workflow` — write/search/read patterns and citation format
- `research-output-format` — writer agent conventions
- `research-search-patterns` — query patterns for research tools

**Audience:** `researcher`, `writer` (auto-loaded for researcher and writer agents)

See `src/agents/references/research-base-*.md`.
