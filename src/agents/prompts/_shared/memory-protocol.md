# Moved to references

This content is now a bundled reference. Use `reference({ name: "memory-usage" })` to load it.

- **Reference:** `memory-usage` (`src/agents/references/memory-usage.md`)
- **Audience:** `all` (auto-loaded for all agents)
