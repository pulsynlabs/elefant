# Moved to references

This content is now a bundled reference. Use `reference({ name: "deviation-rules" })` to load it.

- **Reference:** `deviation-rules` (`src/agents/references/deviation-rules.md`)
- **Audience:** `orchestrator`, `executor` (auto-loaded for orchestrator and executor agents)
