<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG network security config — allows cleartext to localhost and emulator host (10.0.2.2)
  so you can connect to a local daemon during development without TLS.
  Production builds use the release variant which disables cleartext.
-->
<network-security-config>
    <!-- Allow cleartext HTTP to localhost and Android emulator host only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>

    <!-- All other traffic must be HTTPS; trust system + user CAs for enterprise daemons -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>
</network-security-config>
