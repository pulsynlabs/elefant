{
  "name": "elefant",
  "version": "0.1.0",
  "description": "Bun-native agent daemon with Elysia and TypeScript",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/daemon/server-entry.ts",
    "start": "bun run bin/elefant.ts start",
    "stop": "bun run bin/elefant.ts stop",
    "status": "bun run bin/elefant.ts status",
    "build": "bun build --target bun --outdir dist bin/elefant.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tauri-apps/plugin-dialog": "^2.7.0",
    "@vscode/ripgrep": "^1.17.1",
    "elysia": "^1.4.28",
    "vscode-jsonrpc": "8.2.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.12"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "keywords": [
    "agent",
    "daemon",
    "llm",
    "bun",
    "elysia"
  ],
  "license": "MIT"
}
